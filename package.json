{
  "name": "racketsls",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "vue:dev": "cd src/vue && npm run serve",
    "sls:dev": "sls offline --stage dev",
    "sls:deploy": "sls deploy function -f api --stage prod",
    "docker:build": "docker build -t racket-sls-container .",
    "api:watch": "node ./src/scripts/watcher.js",
    "api:build": "raco exe --orig-exe -vo ./dev/application ./src/api/app.rkt",
    "api:build:prod": "node ./src/scripts/deploy.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.0.0",
    "node": "^12.1.0",
    "serverless-offline": "^4.10.0"
  },
  "devDependencies": {
    "node-watch": "^0.6.2",
    "shelljs": "^0.8.3"
  }
}