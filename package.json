{
  "name": "racketsls",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "docker:build": "docker build -t racket-sls-container .",
    "secrets": "travis encrypt-file secrets/secrets.yml secrets/secrets.yml.enc",
    "sls": "serverless offline --stage dev",
    "develop": "lerna run dev",
    "dev": "npm-run-all --parallel sls develop",
    "watch": "lerna run --parallel watch",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node": "^12.1.0",
    "npm-run-all": "^4.1.5",
    "serverless": "^1.59.2",
    "serverless-finch": "^2.4.3"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.3",
    "lerna": "^3.19.0",
    "serverless-offline": "^5.10.1"
  }
}
